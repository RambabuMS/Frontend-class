What is Nodejs
sync vs async
Architecture of Nodejs

https://kamronbekshodmonov.github.io/JELoop-Visualizer/
loupe event loop - site search
js visualizer

python tutor

JS basics with examples
sync and async

✅ JavaScript is Single-Threaded
✅ Event Loop Full Explanation
✅ Call Stack
✅ Web APIs
✅ Callback Queue
✅ Microtask Queue
✅ How Callbacks, Promises, Async/Await behave in the Event Loop

01.04.2026

Node vs Browser JS
Feature  	     Browser	Node
DOM	            Yes	    No
Window Object 	Yes	    No
File System	    No	    Yes
Backend	        No	    Yes

Nodejs - don't have DOM, window.

steps:
create - npm init -y
to run - node server.js
convert(support) to ES6 - type: module 

in-built modules:
os
fs
http

to make the server listen - nodemon

Task:
File based Notes App
features: 
Add note => save in File
get notes => read file

readfileSync vs readfile
writefileSync vs writefile

=> Problems in readfileSync
-> Slower performance
-> Blocks server
-> Not scalable

Blocking vs Non Blocking

https://sqlbolt.com/

promise types

execution - sequence

problems based on promise, callback, async await

Nested -> Hard to read -> Hard to debug -> Hard to maintain

Callback hell 

types of promise:
Promise.all()
=> Runs all promises
=> Fails if any promise fails

Promise.allSettled()
=> waits for all
=> Returns success + failure

Promise.race()
=> returns the first promise completed

Promise.any()
=> returns first success
=> ignores failures

Error handling:

callback => if else
promise => 
promise
.then(res => console.log(res))
.catch(err => console.log(res))
async => 

async function test() {
  try {
    const data = await promise;
  }catch (err){
    console.log(err);
  }
}

tasks:
1. Sequential Execution
function task(name, time){
  return new Promise((resolve) => {
    setTimeout(() => {
      console.log(name);
      resolve();
    }, time)
  });
}

2. Multiple API Calls with Error
const p1 = Promise.resolve("Data1");
const p2 = Promise.reject("Error");
const p3 = Promise.resolve("Data3");
Questions:
Using Promise.all → what happens?
Using allSettled → output?

Promise.prototype.finally()

Promise.prototype.finally is a method that allows you to execute code regardless of whether a promise is fulfilled or rejected. It’s called when a promise is either resolved or rejected, and it’s used to perform cleanup or other tasks that need to happen in either case.

Database - SQL and MongoDB

tasks:

https://nosy-layer-28c.notion.site/Nodejs-tasks-33c3891562188027af04d0f35f6d18b6